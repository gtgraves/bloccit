You've created "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

<%= @post.body %>

You will receive email notifications if any comments are left

Visit online at: <%= topic_post_url(@post.topic, @post) %>
